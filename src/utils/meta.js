const directions = {
    up: "up",
    down: "down",
    left: "left",
    right: "right"
};

const keys = {
    'w': directions.up,
    'a': directions.left,
    'd': directions.right,
    's': directions.down
};

export { directions, keys };